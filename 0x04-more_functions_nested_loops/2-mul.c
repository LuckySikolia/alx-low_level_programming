#include "main.h"
#include <stdio.h>

/**
*mul- multiplies two integers
*
*@a: first integer
*@b: second integer
*
*Return: result of a * b
**/

int mul(int a, int b)
{
	if ((a >= '0' && a <= '9') || (b >= '0' && b <= '9'))
	{
		a * b;
	}
}
