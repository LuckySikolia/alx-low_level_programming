#include <stdio.h>

/**
 * main - causes an infinate loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Infinate loop incoming :(\n");

	i = 0;

/**
 * 	while (1 < 10)
 * 	{
 * 		putchar(i);
 * 	}
 */

	printf("Infinate loop avoided! \\o/n");

	return (0);
}
